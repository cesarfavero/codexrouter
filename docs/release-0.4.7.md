# CodexRouter 0.4.7

When creating an account profile, the desktop app now offers two explicit authentication choices: reuse the existing local Codex session from `~/.codex`, or start a new official browser login. Local-session reuse fails with a clear message when no valid session is available.
