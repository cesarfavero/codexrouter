# CodexRouter 0.4.15

This release stops the desktop launcher from opening `codex app` with the user's home directory as its workspace. On macOS it opens the official Codex app without inventing a new project; an explicit `CODEXROUTER_PROJECT_DIR` is still honored when configured.
