# CodexRouter 0.4.4

This release makes `CodexRouter` the single visible entry point for automatic account switching. Account-qualified model aliases are no longer shown in the Codex picker. Native Codex models from the active account remain available, while selecting `CodexRouter` switches between healthy configured accounts when usage is exhausted or nearly exhausted.
