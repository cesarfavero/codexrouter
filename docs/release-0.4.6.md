# CodexRouter 0.4.6

The first account can now reuse an existing official Codex session from `~/.codex`. CodexRouter copies the local session into the account's isolated profile and keeps separate profiles for additional accounts. If no valid session exists, the normal official browser login opens as before.
