# CodexRouter 0.4.43

- Discover selectable native models dynamically from each account's current Codex CLI catalog instead of relying on a version-specific slug list.
- Preserve valid manually selected models and prefer an efficient available variant for automatic account defaults.
- Rename the managed picker entry in user-facing setup text to Router.
- Update the scheduled High CRM automation to use `codexrouter/gateway` (Router) as its model default.
